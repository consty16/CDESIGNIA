[functions]
  node_bundler = "esbuild"

[functions."procesar-ia"]
  timeout = 26

[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false